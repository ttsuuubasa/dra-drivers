{{- if .Values.webhook.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dra-driver-model-cache.fullname" . }}-webhook
  namespace: {{ include "dra-driver-model-cache.namespace" . }}
  labels:
    {{- include "dra-driver-model-cache.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
spec:
  selector:
    {{- include "dra-driver-model-cache.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
  ports:
  - name: webhook
    protocol: TCP
    port: {{ .Values.webhook.servicePort }}
    targetPort: webhook
{{- end }}
